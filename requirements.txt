# LogLite dependencies
# Python 3.11 / 3.12 (Colab)
# Do NOT reinstall torch/torchvision on Colab — pre-installed versions are CUDA-matched

# ML — pinned for Colab Python 3.12 compatibility
# datasets>=4.8.4 fixes torchvision VideoReader import error
# accelerate>=0.32.0 required by peft (clear_device_cache added in 0.32.0)
# numpy==1.26.4 avoids numpy 2.x breaking changes
transformers==4.51.3
datasets==4.8.5
accelerate==1.3.0
peft==0.14.0
scikit-learn>=1.4.0

# Claude agent
anthropic>=0.30.0

# Utilities
python-dotenv>=1.0.0
pandas>=2.2.0
matplotlib>=3.8.0

# Testing & linting
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0

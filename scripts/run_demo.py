"""
scripts/run_demo.py

End-to-end demo: load model → compute anomaly score → identify triggers → explain.
Runs offline by default (USE_CACHED_RESPONSE = True).

Usage:
  python scripts/run_demo.py               # offline, no API key needed
  USE_CACHED_RESPONSE=false python scripts/run_demo.py  # live Claude API call
"""
# Implemented in M8. Stub present so repo structure is complete from M1.
print("run_demo.py: stub — implement in M8.")

# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Required for live agent mode (M5+). Not needed when USE_CACHED_RESPONSE=True.
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Metrics are saved to results/metrics/ as JSON — no external tracking service needed.
